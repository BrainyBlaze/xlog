# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.toc
*.lof
*.lot
*.nav
*.snm
*.vrb

# Figure build temp + generated figure PDFs (regenerate via figures/make_results.py)
figures/*.tmp
figures/results.pdf

# main.pdf is the committed deliverable and IS tracked (rebuild via latexmk
# when sources change); everything else the build emits stays untracked.
