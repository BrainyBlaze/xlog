# xlog v0.5.0 Technical Whitepaper — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Write a 7000-8000 word technical report positioning xlog as the first GPU-native unified logic programming platform, targeting ML/NeSy researchers.

**Architecture:** Nine sections written sequentially into `docs/whitepaper-v050.md`. Each task writes one section, verifies claims against source material, and commits. Diagrams use ASCII art (mermaid optional).

**Tech Stack:** Markdown. Source material from `docs/`, `CHANGELOG.md`, `examples/neural/results/`, benchmark reports.

**Constraints:**
- Every performance number must cite a source file from the repo
- Every feature claim must reference a crate or module that implements it
- Code examples must be copied from actual example files, not invented
- Unshipped features only in Section 9, clearly labeled

---

## Source Material Index

These files contain the data referenced throughout the plan:

| Label | Path |
|-------|------|
| ARCH | `docs/ARCHITECTURE.md` |
| BENCH | `docs/BENCHMARKS.md` |
| CHANGELOG | `CHANGELOG.md` |
| MC-REPORT | `docs/reports/2026-03-10-mc-runtime-optimization-report.md` |
| GA-REPORT | `docs/reports/2026-03-05-ga-runtime-closure-report.md` |
| DPL-BASELINE | `docs/reports/2026-02-10-deepproblog-baseline-results.md` |
| DPL-GPU | `docs/reports/2026-02-10-deepproblog-baseline-gpu-sequential.md` |
| CACHE-EVIDENCE | `examples/neural/results/evidence/cache_ablation_20260218.json` |
| TRACK-A | `examples/neural/results/track_a/20260216T145409Z_track_a_dev/` |
| MINIMAL-XLOG | `examples/neural/01_minimal/minimal.xlog` |
| MINIMAL-TRAIN | `examples/neural/01_minimal/train.py` |
| WET-COND | `examples/prob/01-wet-conditioning.xlog` |
| TC-EXAMPLE | `examples/xlog/00-basics/01_tc_reachability.xlog` |
| ROADMAP | `docs/ROADMAP.md` |
| DESIGN-SPEC | `docs/superpowers/specs/2026-03-28-v050-whitepaper-design.md` |

---

## Task 1: Create file scaffold and write Section 1 (Introduction)

**Files:**
- Create: `docs/whitepaper-v050.md`

**Context:** The introduction frames the problem (CPU bottleneck in neural-symbolic systems), states xlog's answer (unified GPU-native platform), and lists 5 contributions. ~600 words.

- [ ] **Step 1: Read source material for claims**

Read these files to extract exact claims for the introduction:
- `CHANGELOG.md` — scan the v0.5.0 section for the feature summary
- `docs/ARCHITECTURE.md` — read the opening paragraphs for system positioning
- `README.md` — read the project description and feature table

- [ ] **Step 2: Write Section 1**

Create `docs/whitepaper-v050.md` with the document header and Section 1:

```markdown
# xlog: A GPU-Native Datalog Engine for Unified Symbolic Reasoning

**Version:** 0.5.0 | **Date:** March 2026

## Abstract

[3-4 sentence abstract: problem, approach, key results]

## 1. Introduction

[~600 words following the design spec:
- Opening problem: symbolic vs neural AI gap, CPU bottleneck
- The gap: no system does all of {deterministic, probabilistic, KC, SAT, neural} on GPU
- xlog's answer: unified GPU-native Datalog, 4 paradigms, ML interop
- 5 contribution bullets
- Paper roadmap paragraph]
```

Ground each contribution bullet in a specific crate/module:
1. GPU-resident semi-naive → `xlog-runtime` + `xlog-cuda`
2. GPU knowledge compilation → `xlog-prob` (PIR→CNF→D4→XGCF pipeline)
3. Differentiable neural-symbolic → `xlog-neural` + `pyxlog` training API
4. Zero-copy interop → `xlog-cuda` DLPack/Arrow modules
5. Differentiable ILP → `pyxlog` dILP trainer + `xlog-cuda` ILP kernels

- [ ] **Step 3: Verify no ungrounded claims**

For each contribution bullet, confirm the referenced module exists:
```bash
ls crates/xlog-runtime/src/ crates/xlog-cuda/src/ crates/xlog-prob/src/ crates/xlog-neural/src/ crates/pyxlog/src/
```

- [ ] **Step 4: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add scaffold and Section 1 (Introduction)"
```

---

## Task 2: Write Section 2 (Architecture)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~1200 words covering system overview, compilation pipeline, GPU residency model, and IR stack. Includes the crate dependency diagram.

- [ ] **Step 1: Read source material**

Read these files for architecture details:
- `docs/ARCHITECTURE.md` — crate descriptions, dependency graph, data flow
- `Cargo.toml` (root) — workspace members list
- `crates/xlog-logic/src/lib.rs` — parser/stratifier/lowerer exports
- `crates/xlog-runtime/src/executor/mod.rs` — executor structure
- `crates/xlog-ir/src/rir.rs` — RIR node types
- `crates/xlog-prob/src/compilation/gpu_d4/mod.rs` — D4 pipeline entry

- [ ] **Step 2: Write Section 2.1 (System Overview)**

ASCII crate dependency diagram showing 5 tiers. Use the actual dependency edges from `Cargo.toml` files — don't invent connections.

Format:
```
Tier 4 (Interfaces):    pyxlog    xlog-cli    xlog-cuda-tests
                           |         |              |
Tier 3 (Reasoning):    xlog-gpu   xlog-prob        |
                         / \       / | \            |
Tier 2 (Subsystems):  xlog-logic  xlog-runtime  xlog-solve
                         \    |    /       |
Tier 1 (IRs):         xlog-ir  xlog-cuda  xlog-stats  xlog-neural
                           \      |       /
Tier 0 (Core):              xlog-core
```

Verify each edge by checking the `[dependencies]` section of the downstream crate's `Cargo.toml`.

- [ ] **Step 3: Write Section 2.2 (Compilation Pipeline)**

Describe the Source→Parser→Stratifier→Lowerer→Optimizer→Plan pipeline. Reference:
- `xlog-logic/src/parser.rs` for PEG parsing
- `xlog-logic/src/stratify.rs` for SCC-based stratification
- `xlog-logic/src/ast.rs` → `xlog-ir/src/rir.rs` for lowering
- `xlog-logic/src/optimizer.rs` for DP join planning

- [ ] **Step 4: Write Section 2.3 (GPU Residency Model)**

Key points from the design spec. Reference:
- `host_transfer_stats()` — search for it in `crates/xlog-cuda/src/`
- Zero-D2H enforcement — reference `set_strict_zero_dtoh()` if it exists

- [ ] **Step 5: Write Section 2.4 (IR Stack)**

Describe AST→RIR→PIR→XGCF. For extensibility (approach c), mention that the layered design accommodates future IRs without claiming they exist.

- [ ] **Step 6: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 2 (Architecture)"
```

---

## Task 3: Write Section 3 (GPU-Native Datalog Execution)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~1000 words covering semi-naive evaluation, kernel design, join planning, reversible symbols.

- [ ] **Step 1: Read kernel source files for accurate descriptions**

Read the first ~30 lines of each kernel for its purpose comment:
- `kernels/join.cu`
- `kernels/sort.cu`
- `kernels/filter.cu`
- `kernels/groupby.cu`
- `kernels/dedup.cu`
- `kernels/set_ops.cu`
- `kernels/scan.cu`
- `kernels/pack.cu`

Also read:
- `crates/xlog-runtime/src/executor/mod.rs` — semi-naive loop structure
- `crates/xlog-runtime/src/executor/recursive.rs` — fixpoint handling
- `crates/xlog-stats/src/manager.rs` — statistics feedback
- `crates/xlog-core/src/types.rs` — ScalarType and symbol representation

- [ ] **Step 2: Write Section 3.1 (Semi-Naive on GPU)**

Describe stratum-ordered execution, delta maintenance on-device, fixpoint loop. Reference `xlog-runtime/src/executor/`.

- [ ] **Step 3: Write Section 3.2 (Kernel Design)**

Brief table of the 8 relational kernels with LOC counts and key design points. Highlight IEEE 754 total ordering in the filter kernel. LOC counts from the kernel files.

- [ ] **Step 4: Write Section 3.3 (Adaptive Join Planning)**

Reference `xlog-logic/src/optimizer.rs` and `xlog-stats/src/manager.rs`. Describe the feedback loop.

- [ ] **Step 5: Write Section 3.4 (Reversible Symbols)**

Reference `xlog-core/src/types.rs`. Mention Arrow dictionary encoding for export.

- [ ] **Step 6: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 3 (GPU-Native Datalog)"
```

---

## Task 4: Write Section 4 (Probabilistic Inference on GPU)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~1500 words. Heaviest section. Covers the knowledge compilation pipeline, circuit caching, MC sampling, WFS.

- [ ] **Step 1: Read source material for exact claims**

Read these for pipeline details:
- `crates/xlog-prob/src/compilation/gpu_pir_intern.rs` — PIR stage
- `crates/xlog-prob/src/compilation/gpu_cnf.rs` — CNF encoding
- `crates/xlog-prob/src/compilation/gpu_d4/mod.rs` — D4 compiler
- `crates/xlog-prob/src/compilation/gpu_cache.rs` — circuit caching
- `crates/xlog-solve/src/gpu_cdcl.rs` — CDCL verifier
- `crates/xlog-prob/src/exact.rs` — forward/backward evaluation
- `crates/xlog-prob/src/mc/mod.rs` — MC sampling

Read these for benchmark numbers:
- `examples/neural/results/evidence/cache_ablation_20260218.json` — cache speedup data (2.74x)
- `docs/reports/2026-03-10-mc-runtime-optimization-report.md` — MC 8.6% improvement
- `docs/BENCHMARKS.md` — exact inference targets

- [ ] **Step 2: Write Section 4.1 (Approach)**

Contrast with ProbLog: same compile-once model, but pipeline runs on GPU.

- [ ] **Step 3: Write Section 4.2 (Knowledge Compilation Pipeline)**

Step-by-step: PIR→CNF→D4→CDCL→XGCF→Eval. Include ASCII pipeline diagram:
```
Probabilistic Program
    |
    v
[PIR Extraction] ──── pir.cu (600 LOC)
    |
    v
[CNF Encoding]  ──── cnf.cu (623 LOC)
    |
    v
[D4 Compilation] ──── d4.cu (2953 LOC)
    |
    v
[CDCL Verification] ── sat.cu (3268 LOC)
    |
    v
[XGCF Circuit]
    |
    v
[Forward / Backward] ── circuit.cu (1632 LOC)
```

For each stage, 2-3 sentences explaining what it does and why it's on GPU.

- [ ] **Step 4: Write Section 4.3 (Circuit Caching)**

Key insight: circuit structure depends on program, not weights. Cite CACHE-EVIDENCE for the 2.74x number. Cite cold-start vs steady-state timing from TRACK-A results.

- [ ] **Step 5: Write Section 4.4 (Monte Carlo Sampling)**

Two methods: rejection, evidence clamping. Cite MC-REPORT for the 8.6% improvement (14.11s→12.90s).

- [ ] **Step 6: Write Section 4.5 (Well-Founded Semantics)**

Brief — WFS for cyclic negation. Reference `crates/xlog-prob/src/wfs.rs` and `examples/prob/04-nonmonotone-mc.xlog`.

- [ ] **Step 7: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 4 (Probabilistic Inference)"
```

---

## Task 5: Write Section 5 (Neural-Symbolic Bridge)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~1200 words. Primary audience section. Covers neural predicates, training loop, term embeddings, dILP.

- [ ] **Step 1: Read source material**

Read these for neural predicate mechanics:
- `crates/xlog-neural/src/registry.rs` — network registration
- `crates/xlog-neural/src/tensor_source.rs` — tensor handling
- `crates/pyxlog/src/lib.rs` — Python training API surface

Read the MNIST addition example:
- `examples/neural/01_minimal/minimal.xlog` — the Datalog program (copy verbatim for the paper)
- `examples/neural/01_minimal/train.py` — the training script (extract key snippets)

Read for benchmark numbers:
- `examples/neural/results/track_a/20260216T145409Z_track_a_dev/01_minimal/` — accuracy, timing
- `docs/reports/2026-02-10-deepproblog-baseline-results.md` — DeepProbLog comparison context

Read for dILP:
- Search `crates/pyxlog/src/` for `IlpTrainer` or dILP-related classes
- `examples/python/ilp_showcase.py` — dILP example code

- [ ] **Step 2: Write Section 5.1 (Neural Predicates)**

Describe `nn/4` syntax, registration, forward/backward flow, GIL release. Reference `xlog-neural/src/registry.rs`.

- [ ] **Step 3: Write Section 5.2 (End-to-End Training Loop)**

This gets the most space. Walk through MNIST addition concretely:
1. Show the `minimal.xlog` program (copied from MINIMAL-XLOG)
2. Show the key training loop snippet (from MINIMAL-TRAIN)
3. Include ASCII training loop diagram:
```
                    ┌─────────────────────────────────┐
                    │         Training Epoch           │
                    │                                  │
  Input Images ──>  │  PyTorch    ──>  Leaf     ──>   │
                    │  Forward         Weights        │
                    │                    │             │
                    │              XGCF Circuit        │
                    │              (cached)            │
                    │                    │             │
                    │  PyTorch    <──  Circuit   <──   │
                    │  Backward        Backward       │
                    │                                  │
                    └─────────────────────────────────┘
```
4. Describe the 4-step cycle: forward→inject→evaluate→backward
5. Report results: 99.07% accuracy, cold/steady timing

All code must be copied from the actual example files, not invented.

- [ ] **Step 4: Write Section 5.3 (Term Embeddings)**

Reference `register_embedding` in `xlog-neural/src/registry.rs`. Brief — 1-2 paragraphs.

- [ ] **Step 5: Write Section 5.4 (Differentiable ILP)**

Reference the dILP trainer, sparse mask API, 6 promotion gates. Clearly label as beta. Use code snippet from `python/examples/ilp_showcase.py` if suitable.

- [ ] **Step 6: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 5 (Neural-Symbolic Bridge)"
```

---

## Task 6: Write Section 6 (Interoperability)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~600 words. Shorter section covering DLPack, Arrow, Python bindings, PyTorch autograd.

- [ ] **Step 1: Read source material**

- `crates/xlog-cuda/src/dlpack.rs` — DLPack implementation
- `crates/xlog-cuda/src/arrow_device.rs` — Arrow device interface
- `crates/pyxlog/python/pyxlog/_native.pyi` — Python API surface (type stubs)
- `crates/pyxlog/python/pyxlog/__init__.pyi` — public Python API

- [ ] **Step 2: Write Section 6**

Four subsections (6.1-6.4) as described in the design spec. Keep each to 1-2 paragraphs. Reference the actual module paths for each feature.

- [ ] **Step 3: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 6 (Interoperability)"
```

---

## Task 7: Write Section 7 (Evaluation)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~1000 words. Absolute benchmarks, qualitative comparison table, CUDA certification.

- [ ] **Step 1: Read ALL benchmark source files**

This is critical — every number must come from a file:
- `docs/BENCHMARKS.md` — deterministic and probabilistic targets
- `examples/neural/results/evidence/cache_ablation_20260218.json` — cache speedup
- `examples/neural/results/track_a/20260216T145409Z_track_a_dev/01_minimal/` — neural training results
- `docs/reports/2026-03-10-mc-runtime-optimization-report.md` — MC optimization
- `docs/reports/2026-03-05-ga-runtime-closure-report.md` — GA optimization
- `docs/reports/2026-02-10-deepproblog-baseline-results.md` — DeepProbLog baseline
- `CHANGELOG.md` — any additional performance claims

Extract exact numbers with their source file for citation.

- [ ] **Step 2: Write Section 7.1 (Methodology)**

Hardware, measurement approach, confidence intervals. Source the hardware description from existing reports.

- [ ] **Step 3: Write Section 7.2 (Absolute Performance)**

Three subsections: deterministic, probabilistic, neural-symbolic. Each number must have an inline citation like `(see docs/BENCHMARKS.md)` or `(cache_ablation_20260218.json)`.

Format as tables where possible:

```markdown
| Benchmark | Metric | Result | Source |
|-----------|--------|--------|--------|
| Transitive closure (100K edges) | throughput | >1M rows/sec | BENCHMARKS.md |
| ... | ... | ... | ... |
```

- [ ] **Step 4: Write Section 7.3 (Qualitative Comparison)**

The comparison table from the design spec. Add a paragraph after the table citing published numbers from related work:
- GPUlog: 45x speedup (cite the paper)
- VFLog: 200x over CPU column engines (cite the paper)
- DeepProbLog: cite from DPL-BASELINE

Frame as "each solves a piece; xlog unifies on GPU."

- [ ] **Step 5: Write Section 7.4 (CUDA Certification)**

206 tests, 25 categories, 100% passing. Reference `crates/xlog-cuda-tests/`.

- [ ] **Step 6: Verify every number has a source**

Re-read Section 7 and confirm every performance number traces to a file in the repo. If any number is unsourced, either find the source or remove the claim.

- [ ] **Step 7: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 7 (Evaluation)"
```

---

## Task 8: Write Section 8 (Related Work)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~800 words. Four subsections covering GPU Datalog, probabilistic LP, GPU SAT, differentiable ILP.

- [ ] **Step 1: Read existing research notes**

- `docs/research/2026-01-22-gpu-native-compilation-research.md` — GPU compilation landscape
- `docs/research/2026-01-22-xlog-subsystem-analysis.md` — subsystem comparisons
- `docs/reports/2026-02-10-deepproblog-baseline-results.md` — DeepProbLog comparison context

- [ ] **Step 2: Write Section 8**

Four subsections as described in the design spec:
- GPU Datalog (GPUlog, VFLog, mnmgDatalog)
- Probabilistic LP (ProbLog2, DeepProbLog, NeurASP)
- GPU SAT (ParaFROST, FastFourierSAT)
- Differentiable ILP (dILP / Evans & Grefenstette)

Each subsection: 1 paragraph describing the system, 1-2 sentences positioning xlog relative to it.

Close with the positioning paragraph: "each excels in its niche; xlog unifies on GPU."

Use proper academic citation format: `[Author et al., Year]`. The actual bibliography can be a reference list at the end of the paper.

- [ ] **Step 3: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 8 (Related Work)"
```

---

## Task 9: Write Section 9 (Limitations & Future Work)

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** ~500 words. Current limitations and planned directions.

- [ ] **Step 1: Read source material**

- `docs/ROADMAP.md` — planned features and their status
- `CHANGELOG.md` — known issues section if any

- [ ] **Step 2: Write Section 9.1 (Current Limitations)**

5 honest limitations from the design spec:
- NVIDIA GPU required
- All data must fit in GPU memory
- Python batch query u32 coercion
- dILP is beta
- No formal head-to-head benchmarks

- [ ] **Step 3: Write Section 9.2 (Future Work)**

5 planned directions from the design spec, each 1-2 sentences:
- Epistemic logic (xlog-elp)
- Out-of-core execution
- Magic sets
- Multi-GPU
- Incremental parsing

Every item clearly labeled as not yet implemented.

- [ ] **Step 4: Commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): add Section 9 (Limitations & Future Work)"
```

---

## Task 10: Write Abstract, References, and Final Review

**Files:**
- Modify: `docs/whitepaper-v050.md`

**Context:** Write the abstract (now that all sections exist), add reference list, do final review.

- [ ] **Step 1: Write Abstract**

3-4 sentences: problem, approach, key results. Place at the top of the document after the title. The abstract should mention:
- The CPU bottleneck problem in neural-symbolic systems
- xlog's GPU-native unified approach
- Key quantitative result (e.g., 2.74x circuit caching speedup, 99% accuracy on MNIST addition)
- The 4 paradigms unified

- [ ] **Step 2: Write References section**

Collect all cited works into a numbered reference list at the end:

```markdown
## References

[1] Martinez-Angeles, C. A., et al. "GPUlog: ..." (year)
[2] ...
```

Include references for:
- GPUlog, VFLog, mnmgDatalog
- ProbLog2, DeepProbLog, NeurASP
- ParaFROST, FastFourierSAT
- Evans & Grefenstette (dILP)
- D4 compiler paper
- DLPack spec, Arrow spec

- [ ] **Step 3: Full document review**

Read the entire paper end-to-end and check:
1. **Word count** — should be 7000-8000 words. Run: `wc -w docs/whitepaper-v050.md`
2. **Claim verification** — every performance number has an inline source citation
3. **Feature verification** — every feature claim references an existing module
4. **No unshipped features** outside Section 9.2
5. **Code examples** — all copied from actual files, none invented
6. **Narrative coherence** — does the "unified GPU-native platform" + "bridging logic and DL" story hold together?
7. **Spelling/grammar** — scan for obvious issues

- [ ] **Step 4: Fix any issues found in review**

Address any problems identified in Step 3.

- [ ] **Step 5: Final commit**

```bash
git add docs/whitepaper-v050.md
git commit -m "docs(whitepaper): finalize v0.5.0 technical whitepaper"
```

---

## Summary

| Task | Section | Word Target | Key Source Files |
|------|---------|-------------|------------------|
| 1 | Introduction | ~600 | CHANGELOG, ARCHITECTURE, README |
| 2 | Architecture | ~1200 | ARCHITECTURE, Cargo.toml files, lib.rs files |
| 3 | GPU-Native Datalog | ~1000 | kernels/*.cu, executor/, xlog-stats |
| 4 | Probabilistic Inference | ~1500 | gpu_d4/, gpu_cnf.rs, cache_ablation.json, MC-REPORT |
| 5 | Neural-Symbolic Bridge | ~1200 | 01_minimal/, registry.rs, train.py, DPL-BASELINE |
| 6 | Interoperability | ~600 | dlpack.rs, arrow_device.rs, _native.pyi |
| 7 | Evaluation | ~1000 | BENCHMARKS.md, all reports, TRACK-A |
| 8 | Related Work | ~800 | research docs, DPL-BASELINE |
| 9 | Limitations & Future Work | ~500 | ROADMAP, CHANGELOG |
| 10 | Abstract + References + Review | ~200 | entire paper |

**Total: ~8600 words target (will trim in review to 7000-8000)**
