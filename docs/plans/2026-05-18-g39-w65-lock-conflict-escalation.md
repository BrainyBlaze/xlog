# Goal-039 W65 Lock-Conflict Escalation

Date: 2026-05-18.
Branch: `feat/w65-sort-label-propagation-g39`.
Escalation base W65 HEAD: `e9ebf78b87e31c0a596ca4b6b306740131795e03`.
Governing plan: `docs/plans/2026-05-14-supervisor-goal-039.md`.

## Status

**Superseded 2026-05-18.** The user subsequently authorized DTS-DLM
repository mutation for the full production-grade xlog + DTS goal. The halted
state below remains preserved as evidence of the pre-authorization xlog-only
contract conflict. The superseding W65 evidence is
`docs/evidence/2026-05-18-g39-w65-authorized-dts-fix.md`.

G_W65_SORT is halted, not closed. The xlog-side sort-label work is implemented
and covered by focused certs, but M_W65.1 remains RED:

```text
5-doc bounded m37c-prime replay
SORT_WARNINGS=11
warning counts: 4 x 6 calls, 48 x 5 calls
default events: 264 / 264 classified as binding_inactive
```

The RCA at `docs/evidence/2026-05-14-g39-w65-sort-label-rca.md` shows that the
remaining warnings are generated by DTS-DLM support-clause source generation,
not by xlog schema propagation or pyxlog result-boundary metadata.

## Contract Conflict

The active Goal-039 contract requires all of these to hold:

| Plan clause | Requirement | Current evidence |
|---|---|---|
| Lock 14 | `Sort enrichment: N sort-map misses` is an error condition for Phase 2. | M_W65.1 remains RED with `SORT_WARNINGS=11`. |
| G_W65 M_W65.1 | 50-doc m37c-prime replay must produce zero sort-map miss diagnostics. | The bounded 5-doc replay still produces 11 warning calls. |
| G_W65 M_W65.4 | DTS-DLM `xlog_executor.py` must remain unchanged. | No DTS-DLM source mutation was made. |
| Lock 17 | No DTS-DLM repo mutation outside G_PRE instrumentation and G_E2E wheel reinstall. | The root-cause fix would require DTS-DLM source-generation changes, which are out of bounds. |
| Phase-2 DoD | G_W65 M_W65.1-5 must be green before Phase 2 can close. | M_W65.1 is the sole red W65 metric. |

This is a plan-level conflict: xlog can expose correct sort labels, but the
current DTS-DLM consumer path does not consume them and independently emits
source-level partial `support_1` rows that trigger `binding_inactive` sort
defaults.

## Artifact Checklist

| Requirement | Evidence |
|---|---|
| xlog schema adds non-empty labels without breaking existing call sites | `w65_schema_new_assigns_non_default_sort_labels`; `cargo test -p xlog-integration --test test_w65_sort_label -- --nocapture` passes 5/5. |
| Runtime query result labels follow query variables | `w65_runtime_query_result_sort_labels_follow_query_variables`. |
| pyxlog exposes `LogicQueryResult.sort_labels` including typing stub | `w65_pyxlog_logic_query_result_exposes_sort_labels`. |
| DTS-style source semantics prove partial unary row is source-level behavior | `w65_dts_style_support_source_emits_partial_unary_rows_by_semantics`. |
| DTS-DLM source left unchanged | `git -C /home/dev/projects/dts-dlm status --short -- src/dts_dlm/propagate/xlog_executor.py` produced no output. |
| Remaining warning source classified | RCA records `binding_inactive: 264`, all other classes zero. |
| W65 branch halted without advancing to W66 | No `feat/w6-bundle-integration`, `feat/w66*`, or `bench-spike/w66*` branch exists locally. |

## Supervisor Decision Needed

One of the following amendments is required before Goal-039 can advance:

1. Add an authorized DTS-DLM source-generation correction sub-goal before G_W66.
   The minimal fix is to make generated `support_N` clauses body-length aware,
   for example with per-body-length `wmir_body_{pos}` relations or an explicit
   body-length/RId membership guard.
2. Gracefully defer M_W65.1 while preserving the W65 RCA as the lock-conflict
   evidence. This would require amending the Goal-039 DoD because current
   section 5 requires G_W65 M_W65.1-5 green.
3. Keep the current contract unchanged. In that case Goal-039 remains halted at
   G_W65, and no W66/integration/closure/E2E work should proceed.

No board edit, merge, push, tag, W66 branch, or DTS-DLM source change is
included in this escalation artifact.
