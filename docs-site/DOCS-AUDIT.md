# XLOG Docs Audit — against DOCS-PROTOCOL.md

Audited 44 pages: 🔴 35 · 🟡 7 · 🟢 2. One auditor per page (parallel). Zone = worst-section zone.

| # | Page | Genre | Zone | Violations | Effort | Most important fix |
|---|------|-------|------|-----------|--------|--------------------|
| 1 | `architecture/adaptive-indexing.mdx` | architecture | 🔴 red | R5, R6, R4 | medium | Gloss the raw internal terms ("relation-store generation", "provider path", "hot enough") in plain language and give this user-enable-able feature a user-facing guide with a runnable enable-and-confirm example, instead of leaving an unlabeled architecture page as its only home. |
| 2 | `architecture/arithmetic.mdx` | architecture | 🔴 red | R5, R3 | small | Gloss the internal IR name "RIR" (and add a one-line plain-language definition on first use) so the section heading and prose don't lead with an unexplained internal symbol. |
| 3 | `architecture/certification.mdx` | architecture | 🔴 red | R1, R2, R4, R5 | medium | Convert the author-directed instructions (the opening Warning and the "Document that bound" / "Use those labels only..." notes) into plain reader-facing facts and gloss the raw jargon (Clopper-Pearson, epistemic literals, release doctor), and label the page "for contributors." |
| 4 | `architecture/device-runtime.mdx` | architecture | 🔴 red | R5, R4 | small | Gloss the raw glossary term WCOJ (and link it) on first use and add a top-of-page "for contributors" label; the unglossed acronym is the Red trigger, with the implementer-speak "decline to a fallback path" as a secondary fix. |
| 5 | `architecture/epistemic-internals.mdx` | architecture | 🔴 red | R1, R5, R7 | medium | Delete the author-facing "Documentation Rules" section and the "Do not document..." note (R1), and gloss the raw research/internal terms (EIR, FAEEL, Gelfond 1991, WCOJ, world views) on first use. |
| 6 | `architecture/factorized-execution-internals.mdx` | architecture | 🔴 red | R5, R7, R3 | medium | Gloss every internal and research term in plain language on first use (WCOJ, Free Join, range tries, semi-naive delta, count-by-root, witnesses) and split the dense multi-concept sentences so a contributor-reader can follow without the source. |
| 7 | `architecture/factorized-execution.mdx` | architecture | 🔴 red | R1, R2, R3, R4, R5, R6, R8 | large | Create a separate user-facing Guide/Concept page that opens with reader value and shows a runnable example with expected output, and reduce this to a clearly-labeled contributor architecture page stripped of author-facing notes (R1), the release-Warning/counter-led lead (R2/R3), implementer prose (R4), and unglossed jargon (R5). |
| 8 | `architecture/gpu-execution.mdx` | architecture | 🔴 red | R5, R1, R7, Y5 | medium | Gloss every internal term (RIR, WCOJ, Free Join, SCC, factorized delta, no-host-transfer) in plain language on first use вЂ” the page is currently unreadable for anyone who did not build xlog вЂ” and rewrite the author-facing float-ordering note as a reader fact. |
| 9 | `architecture/language-completeness.mdx` | architecture | 🔴 red | R1, R4, R5, R6, R7 | large | Gloss or replace the raw internal jargon (RIR, PIR, magic sets, stratification, "decline"/"fail closed") in plain language and give the user-toggleable features a real Guide/Concept home, since the page is currently written entirely from the compiler-builder's perspective. |
| 10 | `architecture/multi-gpu.mdx` | architecture | 🔴 red | R1, R5 | small | Rewrite the author-facing "Do not promiseвЂ¦" Warning as a reader-facing status note and gloss the raw terms (WCOJ, Free Join) on first use. |
| 11 | `architecture/overview.mdx` | architecture | 🔴 red | R1, R5, R7 | medium | Delete the author-facing editorial instructions (the "Public docs should use..." and "Treat these as real implementation on main, but label them as unreleased..." notes) and gloss the raw internal jargon (RIR, EIR, WCOJ, Free Join, factorized delta, CDCL, magic-set, stratify) so an outside contributor can follow it. |
| 12 | `architecture/query-optimizer.mdx` | architecture | 🔴 red | R1, R3, R5, R7 | medium | Delete or convert the author-facing "Documentation Rules" section (and stray "Do not document it as..." note) into reader-facing facts, since it addresses the doc writer rather than the reader. |
| 13 | `architecture/release-process.mdx` | architecture | 🔴 red | R1, R5 | small | Convert the author-facing instructions ("Do not describe it as a published Rust crate", "Public docs may describe these features only with the main-only label") into reader-facing facts, and gloss/link the raw jargon (WCOJ, Free Join, factorized deltas, Stage-B existential joins). |
| 14 | `architecture/roadmap.mdx` | architecture | 🔴 red | R1, R3, R4, R5 | medium | Gloss or replace the raw internal jargon (RIR/EIR/PIR/SIR, WCOJ, Free Join, factorized recursive deltas, Stage-B existential joins) and strip the maintainer-facing docs-process notes so an outside reader can actually parse the roadmap. |
| 15 | `architecture/solver-services.mdx` | architecture | 🔴 red | R1, R3, R4, R5 | large | Delete the author-directed "Documentation Boundaries" note and rewrite the page from the reader's POV, glossing CNF/CDCL/SAT/CSR and dropping implementer contract-speak ("decline fail-closed", "consumer owns the semantic contract"). |
| 16 | `architecture/wcoj.mdx` | architecture | 🔴 red | R1, R4, R5, R6, R7 | large | Create a value-first Guide/Concept page for WCOJ with a runnable example and confirm-it-worked signal, since a user-enabled performance feature must not live only on an internals-facing Architecture page (and strip the author-facing "What Not To Claim" notes and unglossed jargon). |
| 17 | `core-concepts/how-xlog-works.mdx` | concept | 🔴 red | R5, R7, Y1 | medium | Gloss the remaining raw research/internal terms (semi-naive fixpoint, stratification/SCC, PIR/EIR/SIR, world-view) in plain language and split the dense Stratify sentence into idea-then-mechanism. |
| 18 | `epistemic/overview.mdx` | concept | 🔴 red | R1, R4, R5, R7 | medium | Gloss the raw research terms (FAEEL, Gelfond-1991, founded support) in plain language and delete the author-facing Warning that corrects "earlier design notes," which no outside reader has seen. |
| 19 | `get-started/installation.mdx` | guide | 🔴 red | R5, R4, R7 | medium | Gloss or cut the raw CUDA-internals jargon (PTX, cubin, portable PTX) and move the git-tracking policy and runtime kernel-resolution order into a labeled Reference/Diagnostics note instead of the main install flow. |
| 20 | `get-started/quickstart.mdx` | guide | 🔴 red | R5, Y1 | medium | Add a reader-facing value/"when to use" opening and gloss-or-cut the raw research terms (exact_ddnnf/d-DNNF, mc, magic sets, approximate inference, epistemic reasoning) so an outside reader is not hit with unglossed jargon. |
| 21 | `guides/benchmarking.mdx` | guide | 🔴 red | R5, R4 | medium | Gloss the raw research/internal terms in plain language on first use (Decision-DNNF, PTX/cubin/SM120, LCG, RuntimeConfig, dispatch counter) and reframe the harness-internals prose from the reader's point of view rather than the implementer's. |
| 22 | `guides/diagnostics.mdx` | guide | 🔴 red | R4, R5, R8 | medium | Add a concrete runnable example with real `xlog explain` output and gloss the raw jargon (SCC, magic-set) in plain language, instead of leading each section with Rust struct dumps. |
| 23 | `guides/wcoj-tuning.mdx` | guide | 🔴 red | R4, R5, R7, R8 | medium | Gloss the raw factorization terms (Free Join, factorized/recursive delta, semi-naive) and reframe the pervasive implementer vocabulary (promoter, "dispatcher declines", "MultiWayJoin plan node", "fallback parity") into reader-facing behavior, backed by one runnable command whose --stats output the reader can actually see. |
| 24 | `language-guide/lists-and-meta.mdx` | guide | 🔴 red | R5, R4, Y1 | medium | Gloss or cut the raw internal-compiler terms ("meta-normalization", "interned" before its definition, "before a single kernel runs") and add a reader-facing "how do I know it worked" signal with expected output, so an outside reader never hits unexplained implementer jargon. |
| 25 | `language-guide/pragmas.mdx` | guide | 🔴 red | R5, Y1 | small | Gloss the raw research/engine terms in plain language on first use (exact_ddnnf/d-DNNF, mc/Monte Carlo, epistemic_mode, g91/Gelfond-1991, faeel), since they appear in the table and prose with no meaning for an outside reader. |
| 26 | `neural/exact-induction.mdx` | guide | 🔴 red | R4, R5, R7, Y1 | medium | Replace the CUDA-internals body (grid/atomics/transfer-count and the test-accounting note) with a "when to use it vs differentiable ILP" section and a "how to confirm it worked" example with expected output, moving the kernel/transfer internals to a Diagnostics or Reference section. |
| 27 | `neural/rule-learning.mdx` | guide | 🔴 red | R7, R5, R1, R8 | medium | Split the four-concept "straight-through-thresholded ... soft truth mass" sentence (the protocol's own R7 example) into idea/mechanism/caveat and gloss the raw jargon (XGCF, noisy-OR, ClopperвЂ“Pearson) around it. |
| 28 | `probabilistic/engines.mdx` | concept | 🔴 red | R1, R4, R5 | medium | Delete the maintainer-facing notes ("Honest residencyвЂ¦ do not cite it as such" and the d4/d-DNNF "never vendors" paragraph) and gloss the internal compilation jargon (XGCF, PIR, Decision-DNNF, CDCL, Tseitin, weighted model counting) in plain language on first use. |
| 29 | `reference/cli.mdx` | reference | 🔴 red | R5 | small | Gloss or glossary-link the research terms dropped raw in the explain enumeration and option tables (WCOJ, stratification, magic-set, d-DNNF, EDB) on first use, since even a reference page may not leave jargon unglossed. |
| 30 | `reference/cuda.mdx` | reference | 🔴 red | R5 | small | Gloss or glossary-link the raw internal/research terms in the Purpose column (XGCF, CDCL) so a reader can understand the table without the source. |
| 31 | `reference/environment-variables.mdx` | reference | 🔴 red | R5, R7, R4 | medium | Gloss the raw research/internal terms (WCOJ, Free Join, factorized/recursive delta, CDCL, D4, semi-naive fixpoint) in plain language with glossary links on first use, and split the few entries that stack 3+ unexplained concepts. |
| 32 | `reference/errors.mdx` | reference | 🔴 red | R4, R5, R7 | medium | Rewrite the D4/CDCL/CNF "unreleased" budget rows in plain reader terms вЂ” the CompileCapacityExceeded cell alone stacks four unglossed internal concepts (D4, CNF, emit buffers, context-poisoning CUDA error). |
| 33 | `reference/language.mdx` | reference | 🔴 red | R2, R4, R5, R7 | large | Replace the jargon-dense "Release context / Language coverage" opening (and the epistemic executor prose) with a value-first lead that glosses or cuts internal terms like FAEEL, EIR, WFS, and Gelfond 1991 instead of stacking them raw. |
| 34 | `reference/python.mdx` | reference | 🔴 red | R1, R4, R5, R7 | large | Gloss or cut the raw research/internal jargon (Belnap, SCC, "external consumer", control-plane, monotone materialized output) and convert the implementer-facing contract prose into plain user behavior. |
| 35 | `reference/rust.mdx` | reference | 🔴 red | R2, R5 | small | Replace the opening docs-build-pipeline plumbing (Mintlify export, temp build dir, rustdoc-only CUDA mode) with a one-line reader value prop вЂ” "browse the generated Rust API for each xlog crate here" вЂ” and gloss or drop the raw internal terms (PTX/cubin, DLPack, Decision-DNNF, stratification, CDCL) that a first-time reader can't parse. |
| 36 | `core-concepts/gpu-residency.mdx` | concept | 🟡 yellow | Y1, Y2, Y6 | medium | Add a minimal runnable example plus an observable "how do I confirm residency held" signal, and gloss the raw terms (delta, fixpoint, circuit values). |
| 37 | `guides/interop.mdx` | guide | 🟡 yellow | Y1, Y3 | small | Add expected output to the examples and a concrete "how do I know it stayed on the GPU" confirmation signal so the third user question is answered. |
| 38 | `index.mdx` | landing | 🟡 yellow | Y3, Y6 | small | Replace the implementer-framed hedge "zero tracked hostвЂ“device transfers in production data planes" with plain reader language (e.g. "symbolic reasoning stays on the GPU, so training avoids the CPUвЂ“GPU copies that slow other systems"). |
| 39 | `language-guide/aggregation.mdx` | guide | 🟡 yellow | Y1 | small | Add one minimal runnable example (facts + query + expected output) plus a "confirm it worked" signal, since the page shows only rule fragments and never a result the reader can verify. |
| 40 | `language-guide/arithmetic-and-functions.mdx` | guide | 🟡 yellow | Y1 | small | Add expected outputs to at least one complete runnable example so the reader can confirm a computation worked, since every code block is a fragment with no observable result. |
| 41 | `language-guide/facts-and-rules.mdx` | guide | 🟡 yellow | Y6 | small | Add a one-line plain gloss for "fixpoint" at first use (line 86) and for "semi-naive fixpoint" (line 131), linking the glossary, since both are dropped raw and "semi-naive fixpoint" is an explicit must-gloss term. |
| 42 | `language-guide/negation.mdx` | guide | 🟡 yellow | Y1, Y6 | small | Gloss the raw terms "well-founded" and "fixpoint" (or link the glossary) and add expected output for the isolated(X) query so the success path has an observable signal. |
| 43 | `language-guide/modules.mdx` | guide | 🟢 green |  | small | Optionally add a small end-to-end multi-file example with expected output to fully satisfy G3, but the page already clears the bar. |
| 44 | `language-guide/recursion.mdx` | guide | 🟢 green |  | small | Ships as-is; optional polish is to hyperlink the inline-glossed terms (semi-naive fixpoint, delta, stratification) to a central glossary to fully satisfy G4. |
