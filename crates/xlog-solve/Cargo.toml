[package]
name = "xlog-solve"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "GPU-accelerated solver for XLOG Datalog evaluation"
readme = "../../README.md"
keywords = ["logic", "probabilistic", "solver", "datalog", "gpu"]
categories = ["science", "mathematics"]

[dependencies]
xlog-core = { path = "../xlog-core" }
xlog-cuda = { path = "../xlog-cuda" }
cudarc.workspace = true

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "solver_bench"
harness = false
