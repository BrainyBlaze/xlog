[package]
name = "xlog-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Runtime execution engine for XLOG logic programs"
readme = "../../README.md"
keywords = ["logic", "probabilistic", "runtime", "datalog", "gpu"]
categories = ["science", "mathematics"]

[dependencies]
cudarc = { workspace = true }
xlog-core = { path = "../xlog-core" }
xlog-ir = { path = "../xlog-ir" }
xlog-cuda = { path = "../xlog-cuda" }
xlog-stats = { path = "../xlog-stats" }

[dev-dependencies]
xlog-logic = { path = "../xlog-logic" }
bytemuck = "1.14"
