# pyxlog Session Compatibility Fixture

This fixture keeps the runtime examples tied to the public pyxlog session use
case: relation facts, session-style deltas, aggregate output, and ordinary
query execution through the same `.xlog` language surface used by earlier
public examples.
