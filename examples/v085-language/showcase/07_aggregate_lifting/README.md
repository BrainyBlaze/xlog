# Aggregate Lifting

Uses an 8-row finite uncertain count domain so the exact cardinality dynamic
programming lift is visible in explain diagnostics while the example remains
small enough for direct `xlog prob` validation.
