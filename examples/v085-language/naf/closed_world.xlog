pred node(id: u32).
pred edge(src: u32, dst: u32).
pred blocked(id: u32).

node(1).
node(2).
node(3).
edge(1, 2).
blocked(2).

leaf(X) :- node(X), not edge(X, _).
unblocked(X) :- node(X), not blocked(X).

?- unblocked(X).
