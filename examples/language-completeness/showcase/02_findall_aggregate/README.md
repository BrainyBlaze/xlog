# Findall Aggregate

Combines finite source-fact `findall/3`, list length, and a deterministic
aggregate head over the same relation. The aggregate result predicate declares
its `u64` count column so the example runs as a positive showcase.
