# Repl Watch Explain

Provides one small program for the parser-session CLI surfaces: explain JSON,
REPL stdin parsing, and `watch --once --explain`. The recursive relation is
typed explicitly so `xlog run` remains a positive execution path.
