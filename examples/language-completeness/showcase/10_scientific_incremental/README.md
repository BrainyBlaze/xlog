# Scientific Incremental

Uses scientific/engineering predicate names to combine finite typing, safe
NAF, source-fact `findall`, static `maplist`, aggregate counting, and parser
session diagnostics without legacy project terminology. The count aggregate is
declared with a `u64` output column and is checked through successful `xlog run`
output.
