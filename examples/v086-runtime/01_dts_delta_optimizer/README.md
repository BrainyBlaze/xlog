# External Consumer Delta Optimizer Fixture

This fixture uses external consumer-shaped relation names and update batches to exercise
the public parser, planner, aggregate, negation, and repeated-join runtime path.
Transfer and feature metrics are recorded by the validator from committed
runtime evidence.
