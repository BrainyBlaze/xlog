# v0.9.0 Substrate Primitive Fixture

This fixture documents the v0.8.6 runtime primitives that the later
epistemic/solver branch is expected to consume: typed exact pair relations,
reusable join paths, shared subplans, adaptive-safe candidate rules, and
persistent index reuse.
