# Runtime Substrate Primitive Fixture

This fixture documents runtime primitives that later epistemic and solver
work is expected to consume: typed exact pair relations,
reusable join paths, shared subplans, adaptive-safe candidate rules, and
persistent index reuse.
